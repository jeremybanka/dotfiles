home/.config/containers
home/.config/gh/hosts.yml
home/.config/ghc
home/.config/gcloud
home/.config/lnav
home/.config/nushell/history.txt
home/.config/zed/*
!home/.config/zed/settings.json
home/.config/.wrangler
home/.config/carapace
home/.config/raycast/extensions

# Local telemetry state, runtime credentials, and generated caches.
home/.config/openspec/config.json
home/.config/raycast/aster-endpoint.json
home/.config/raycast/node-compile-cache/

# WireGuard profiles include private keys.
home/.config/wireguard/

apps/InDesign/Workspaces/*_CurrentWorkspace.xml

repomix-output.xml

# VM images and migration artifacts can contain injected service credentials.
/vms/artifacts/
*.qcow*
*.img
*.img.*
*.raw
*.raw.*
*.vdi*
*.vmdk*
*.vhd*

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
######################
.env
keys/

# this is where i keep the local guests (vms) that i ssh into
home/.config/ssh/projects

# pnpm thinks secrets are configuration
auth.ini
